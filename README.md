# RAG

## Dataset / Codebase

The `data/sample_repo` folder is not included in this repository due to size.

To run this project:
1. Create a `data/sample_repo` folder
2. Add any codebase you want to analyze
